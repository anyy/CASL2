; 算術シフトはデータを2の補数で表される符号付整数データとしてシフトを行う
; 算術左シフトでシフトすることは、2倍(2^1倍)、4倍(2^2倍)、8倍(2^3倍)することを意味している
; 以下の場合、2ビット左シフトすると2^2倍になるため、-8*4=-32となる
PROG	START
	LD	GR0,AA
	SLA	GR0,2
	ST	GR0,BB
	RET
AA	DC	-8
BB	DS	1
	END